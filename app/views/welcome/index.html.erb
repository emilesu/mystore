<div>
  <% @products.each do |product| %>
    <div class="col-md-2 col-xs-4">
      <%= link_to image_tag(product.image.thumb.url, :class => "img-responsive img-thumbnail"), product_path(product) %>
      <p><%= link_to(product.title, product_path(product)) %></p>
      <p>¥ <%= product.price %> </p>
    </div>
  <% end %>
</div>
